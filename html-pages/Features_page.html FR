<!DOCTYPE html>
<html lang="fr" dir="ltr">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Page des fonctionnalités de l’application Links">
  <title>Fonctionnalités | Links</title>

  <!-- Polices -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Sedgwick+Ave&display=swap" rel="stylesheet">

  <!-- Styles -->
  <style>
    :root {
      --bg-light-blue: #dbf5fa;  /* Fond de la page */
      --blue: #c9f6ff;           /* Bleu clair pour les blocs */
      --text-orange: #ff5a3c;    /* Couleur du texte */
    }

    body {
      background: var(--bg-orange);
      margin: 0;
      font-family: 'Amaranth', sans-serif;
      font-weight: 700;
      font-style: normal;
    }

    h3, h2 {
      font-family: 'Amaranth', sans-serif;
      font-weight: 700;
      font-style: normal;
    }

    /* Bloc d’introduction */
    .intro {
      text-align: center;
      padding: 60px 20px 20px;
      color: white;
      font-size: 1.1rem;
      max-width: 900px;
      margin: 0 auto;
    }

    /* Section principale */
    .features {
      padding: clamp(16px, 4vw, 48px);
    }

    .cards {
      max-width: 1200px;
      margin: 0 auto;
      display: grid;
      gap: clamp(16px, 2.5vw, 28px);
      grid-template-columns: 1fr;
    }

    /* Trois colonnes à partir de la tablette */
    @media (min-width: 900px) {
      .cards {
        grid-template-columns: repeat(3, 1fr);
      }
    }

    /* Style des cartes */
    .card {
      background: var(--blue);
      border: 10px solid var(--blue);
      border-radius: 28px;
      padding: clamp(18px, 2.2vw, 28px);
      color: var(--text-orange);
      box-shadow: 0 6px 0 rgba(0, 0, 0, 0.06);
      text-align: left;
    }

    .card h2 {
      margin: 0 0 10px;
      font-size: clamp(22px, 2.2vw + 8px, 40px);
      line-height: 1.1;
      font-weight: 800;
    }

    .card p {
      margin: 0;
      font-size: clamp(15px, 1.1vw + 10px, 18px);
      line-height: 1.5;
      font-weight: 500;
    }
  </style>
</head>

<body>

  <p style="text-align: center; color:white;">Logo Links à ajouter</p>

  <div class="intro">
    <h3>
      « Nous avons créé Links parce que nous aurions aimé avoir une plateforme comme celle-ci pour nous guider lorsque nous avons commencé l’université.<br><br>
      Pouvoir rencontrer des personnes comme nous, issues de notre communauté, en découvrir de nouvelles et ne pas se sentir seul.<br><br>
      C’est pourquoi nous avons construit Links, pour vous. »
    </h3>
  </div>

  <section class="features">
    <div class="cards">
      <!-- 1) QUE PROPOSONS-NOUS -->
      <article class="card">
        <h2>POUR QUOI</h2>
        <p style="font-family: 'Amaranth', sans-serif; font-weight: 700; font-style: normal;">
          Nous permettons aux étudiants universitaires, qu’ils soient nouveaux ou de retour, de découvrir de nouvelles personnes et de créer des liens basés sur des points communs, des centres d’intérêt partagés et même des cours en commun.
        </p>
      </article>

      <!-- 2) POUR QUI -->
      <article class="card">
        <h2>POUR QUI</h2>
        <p style="font-family: 'Amaranth', sans-serif; font-weight: 700; font-style: normal;">
          Notre public cible est principalement les étudiants de première année.<br>
          Mais la plateforme est conçue pour tous les étudiants de l’Université d’Ottawa.
        </p>
      </article>

      <!-- 3) QUE PROPOSONS-NOUS CONCRÈTEMENT -->
      <article class="card">
        <h2>CE QUE NOUS PROPOSONS</h2>
        <p style="font-family: 'Amaranth', sans-serif; font-weight: 700; font-style: normal;">
          Vous pourrez :
        </p>
        <ul style="font-family: 'Amaranth', sans-serif; font-weight: 700; font-style: normal;">
          <li>Rechercher des personnes et entrer en contact avec elles</li>
          <li>Discuter avec vos amis</li>
          <li>Découvrir des communautés selon votre origine, vos goûts musicaux, vos activités favorites, etc., et participer aux discussions de groupe</li>
          <li>Découvrir les événements à venir</li>
          <li>Obtenir la version Premium pour accéder à encore plus d’opportunités !</li>
        </ul>
      </article>
    </div>

    <h3>Ce site web est créé par des étudiants, pour des étudiants</h3>
  </section>

  <style>
    /* Palette similaire aux captures */
    :root {
      --bg-orange: #ff5a3c;      /* fond de la page */
      --blue: #c9f6ff;           /* bleu clair pour les blocs et bordures */
      --text-orange: #ff5a3c;    /* couleur du texte */
    }

    /* Donne ce fond orange à toute la page */
    body {
      background: var(--bg-orange);
      margin: 0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
    }

    .features {
      padding: clamp(16px, 4vw, 48px);
    }

    .cards {
      max-width: 1200px;
      margin: 0 auto;
      display: grid;
      gap: clamp(16px, 2.5vw, 28px);
      grid-template-columns: 1fr;
    }

    /* 3 colonnes dès la tablette */
    @media (min-width: 900px) {
      .cards {
        grid-template-columns: repeat(3, 1fr);
      }
    }

    .card {
      background: var(--blue);
      border: 10px solid var(--blue);
      border-radius: 28px;
      padding: clamp(18px, 2.2vw, 28px);
      color: var(--text-orange);
      box-shadow: 0 6px 0 rgba(0, 0, 0, 0.06);
    }

    .card h2 {
      margin: 0 0 10px;
      font-size: clamp(22px, 2.2vw + 8px, 40px);
      line-height: 1.1;
      font-weight: 800;
      letter-spacing: 0.2px;
    }

    .card p {
      margin: 0;
      font-size: clamp(15px, 1.1vw + 10px, 18px);
      line-height: 1.5;
      font-weight: 500;
    }
  </style>
</body>

</html>
